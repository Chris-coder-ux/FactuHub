---
alwaysApply: true
---

---
# 🏗️ TypeScript & Estilo de Código - Reglas Estrictas

## 🚫 PROHIBIDO ABSOLUTAMENTE
- ❌ `any` - Usar `unknown` con type guards
- ❌ Default exports - Solo named exports
- ❌ Clases para lógica - Preferir funciones puras

## ✅ PATRONES OBLIGATORIOS

### Tipado
```typescript
// Interface para objetos públicos
interface User {
  id: string;
  isActive: boolean; // Booleanos con prefijo 'is', 'has', 'can'
}

// Type para uniones
type Result<T> = { success: true; data: T } | { success: false; error: string };

// Props de componentes
interface ButtonProps {
  variant: 'primary' | 'secondary';
  isLoading?: boolean;
}

Funciones

// Patrón RORO (Receive Object, Return Object)
function processData({ input, options }: ProcessParams): ProcessResult {
  // Lógica pura
  return { data, status };
}

// Async siempre con try/catch
async function fetchData(): Promise<Result<Data>> {
  try {
    const response = await fetch('/api');
    return { success: true, data: await response.json() };
  } catch (error) {
    return { success: false, error: 'Failed to fetch' };
  }
}

Estructura de Archivos

components/
  feature-name/
    Feature.tsx           # Componente principal
    FeatureList.tsx       # Subcomponente
    FeatureItem.tsx       # Subcomponente
    feature-helpers.ts    # Helpers específicos
    types.ts             # Tipos locales
    index.ts             # Exportaciones

    Nomenclatura
Variables booleanas: isLoading, hasError, canSubmit

Directorios: kebab-case (auth-form, user-profile)

Arrays: plural (users: User[])

Funciones: verbo + sustantivo (validateForm, fetchUsers)

Importaciones

// Absolute imports configuradas con @/
import { Button } from '@/components/ui/button';
import { ApiResponse } from '@/types/api';

// No relative imports profundos
// ❌ import { something } from '../../../../utils';

REGLA CLAVE: Si no puedes definir el tipo, usa unknown y haz type narrowing. Nunca any.
---